import 'package:flutter/material.dart';

class SettingCalendar extends StatelessWidget {
  const SettingCalendar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
