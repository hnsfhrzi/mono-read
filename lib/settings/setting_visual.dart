import 'package:flutter/material.dart';

class SettingVisual extends StatelessWidget {
  const SettingVisual({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
