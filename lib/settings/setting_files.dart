import 'package:flutter/material.dart';

class SettingFiles extends StatelessWidget {
  const SettingFiles({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
